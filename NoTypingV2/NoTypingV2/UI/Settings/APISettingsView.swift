import SwiftUI

struct APISettingsView: View {
    var body: some View {
        Form {
            Section("Speech-to-Text") {
                Text("TODO: STT provider picker + key + baseURL + model + test")
            }
            Section("LLM Polish") {
                Text("TODO: LLM provider picker + key + baseURL + model + test")
            }
        }
    }
}
